import type { FC } from 'react';

import MovieDetailed from '@/components/container/movie/movieDetailed/movieDetailed';

const MovieDetailedPage: FC = () => {
  return (
    <>
      <MovieDetailed />
    </>
  );
};

export default MovieDetailedPage;
